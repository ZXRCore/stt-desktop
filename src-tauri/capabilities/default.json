{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Разрешения десктоп-приложения по умолчанию",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-kill",
    "shell:allow-execute",
    "shell:allow-spawn",
    "updater:default",
    "process:allow-restart"
  ]
}
